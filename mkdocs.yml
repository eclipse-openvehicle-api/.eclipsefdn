site_name: Otterdog configuration @ eclipse-openvehicle-api
site_description: Otterdog configuration @ eclipse-openvehicle-api
strict: false

site_url: https://eclipse-openvehicle-api.github.io/.eclipsefdn/

repo_name: eclipse-openvehicle-api/.eclipsefdn
repo_url: https://github.com/eclipse-openvehicle-api/.eclipsefdn/

docs_dir: ./docs
site_dir: ./_site

theme:
  name: 'material'
  custom_dir: docs/overrides
  features:
    - navigation.tabs

markdown_extensions:
  - pymdownx.superfences
  - pymdownx.tabbed:
      alternate_style: true
  - attr_list
  - pymdownx.emoji:
      emoji_index: !!python/name:materialx.emoji.twemoji
      emoji_generator: !!python/name:materialx.emoji.to_svg

plugins:
  - macros

nav:
  - Overview: index.md

watch: []

extra:
  org_id: eclipse-openvehicle-api

extra_css:
  - stylesheets/extra.css
